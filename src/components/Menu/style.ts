import styled from 'styled-components'

const MenuWrapper = styled.div`
  display: flex;
  .item {
    margin-left: 45px;
    color: #79869f;
    font-weight: 600;
    cursor: pointer;
  }
  .setitem {
    color: #000;
    cursor: pointer;
  }
`

export default MenuWrapper
